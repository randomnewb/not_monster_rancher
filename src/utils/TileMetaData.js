export default class TileMetaData {
  constructor(tilesetImage, index, lifeSkillsType, health, durability) {
    this.tilesetImage = tilesetImage;
    this.index = index;
    this.lifeSkillsType = lifeSkillsType;
    this.health = health;
    this.durability = durability;
  }
}
